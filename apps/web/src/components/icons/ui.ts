/**
 * Single entry point for the Lucide icons used across the docs/portfolio
 * chrome (nav, sidebar, feedback states, pagination). Import from here
 * instead of 'lucide-react' directly so the design system's icon set stays
 * auditable in one place. Registry/example/shadcn-package components are
 * intentionally excluded — they must stay self-contained for distribution.
 */
export {
  ArrowRightIcon,
  ArrowUpRightIcon,
  CheckIcon,
  ChevronDownIcon,
  ChevronLeftIcon,
  ChevronRightIcon,
  ChevronsUpDownIcon,
  CodeIcon,
  CopyIcon,
  EditIcon,
  EyeIcon,
  HouseIcon,
  LanguagesIcon,
  LayoutGridIcon,
  LoaderCircleIcon,
  MenuIcon,
  SearchIcon,
  SidebarIcon,
  SlashIcon,
  TerminalIcon,
  TextIcon,
  SunMoonIcon,
} from 'lucide-react';
